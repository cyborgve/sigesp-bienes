import { DenominacionEstadoUsoPipe } from './denominacion-estado-uso.pipe';

describe('DenominacionEstadoUsoPipe', () => {
  it('create an instance', () => {
    const pipe = new DenominacionEstadoUsoPipe();
    expect(pipe).toBeTruthy();
  });
});
