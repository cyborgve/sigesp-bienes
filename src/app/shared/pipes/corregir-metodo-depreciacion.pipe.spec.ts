import { CorregirMetodoDepreciacionPipe } from './corregir-metodo-depreciacion.pipe';

describe('CorregirMetodoDepreciacionPipe', () => {
  it('create an instance', () => {
    const pipe = new CorregirMetodoDepreciacionPipe();
    expect(pipe).toBeTruthy();
  });
});
