import { DenominacionMarcaPipe } from './denominacion-marca.pipe';

describe('DenominacionMarcaPipe', () => {
  it('create an instance', () => {
    const pipe = new DenominacionMarcaPipe();
    expect(pipe).toBeTruthy();
  });
});
