import { CorregirPorDefectoPipe } from './corregir-por-defecto.pipe';

describe('CorregirPorDefectoPipe', () => {
  it('create an instance', () => {
    const pipe = new CorregirPorDefectoPipe();
    expect(pipe).toBeTruthy();
  });
});
