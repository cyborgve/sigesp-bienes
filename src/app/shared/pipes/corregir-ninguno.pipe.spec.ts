import { CorregirNingunoPipe } from './corregir-ninguno.pipe';

describe('CorregirNingunoPipe', () => {
  it('create an instance', () => {
    const pipe = new CorregirNingunoPipe();
    expect(pipe).toBeTruthy();
  });
});
