import { CorregirSeleccionarPipe } from './corregir-seleccionar.pipe';

describe('CorregirSeleccionarPipe', () => {
  it('create an instance', () => {
    const pipe = new CorregirSeleccionarPipe();
    expect(pipe).toBeTruthy();
  });
});
