import { DenominacionSedePipe } from './denominacion-sede.pipe';

describe('DenominacionSedePipe', () => {
  it('create an instance', () => {
    const pipe = new DenominacionSedePipe();
    expect(pipe).toBeTruthy();
  });
});
