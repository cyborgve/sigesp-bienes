import { DenominacionTipoMarcaPipe } from './denominacion-tipo-marca.pipe';

describe('DenominacionTipoMarcaPipe', () => {
  it('create an instance', () => {
    const pipe = new DenominacionTipoMarcaPipe();
    expect(pipe).toBeTruthy();
  });
});
