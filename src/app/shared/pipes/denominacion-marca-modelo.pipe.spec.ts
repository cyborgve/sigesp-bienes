import { DenominacionMarcaModeloPipe } from './denominacion-marca-modelo.pipe';

describe('DenominacionMarcaModeloPipe', () => {
  it('create an instance', () => {
    const pipe = new DenominacionMarcaModeloPipe();
    expect(pipe).toBeTruthy();
  });
});
