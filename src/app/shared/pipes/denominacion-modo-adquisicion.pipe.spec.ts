import { DenominacionModoAdquisicionPipe } from './denominacion-modo-adquisicion.pipe';

describe('DenominacionModoAdquisicionPipe', () => {
  it('create an instance', () => {
    const pipe = new DenominacionModoAdquisicionPipe();
    expect(pipe).toBeTruthy();
  });
});
