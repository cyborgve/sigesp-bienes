import { CorregirTipoActivoPipe } from './corregir-tipo-activo.pipe';

describe('CorregirTipoActivoPipe', () => {
  it('create an instance', () => {
    const pipe = new CorregirTipoActivoPipe();
    expect(pipe).toBeTruthy();
  });
});
