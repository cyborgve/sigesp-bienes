import { CorregirCodigoPipe } from './corregir-codigo.pipe';

describe('CorregirCodigoPipe', () => {
  it('create an instance', () => {
    const pipe = new CorregirCodigoPipe();
    expect(pipe).toBeTruthy();
  });
});
