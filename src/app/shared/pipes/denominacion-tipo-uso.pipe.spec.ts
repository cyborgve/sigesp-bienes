import { DenominacionTipoUsoPipe } from './denominacion-tipo-uso.pipe';

describe('DenominacionTipoUsoPipe', () => {
  it('create an instance', () => {
    const pipe = new DenominacionTipoUsoPipe();
    expect(pipe).toBeTruthy();
  });
});
