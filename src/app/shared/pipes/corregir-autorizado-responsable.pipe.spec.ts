import { CorregirAutorizadoResponsablePipe } from './corregir-autorizado-responsable.pipe';

describe('CorregirAutorizadoResponsablePipe', () => {
  it('create an instance', () => {
    const pipe = new CorregirAutorizadoResponsablePipe();
    expect(pipe).toBeTruthy();
  });
});
