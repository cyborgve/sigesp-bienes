import { DenominacionMonedaPipe } from './denominacion-moneda.pipe';

describe('DenominacionMonedaPipe', () => {
  it('create an instance', () => {
    const pipe = new DenominacionMonedaPipe();
    expect(pipe).toBeTruthy();
  });
});
