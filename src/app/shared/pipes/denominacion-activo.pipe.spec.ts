import { DenominacionActivoPipe } from './denominacion-activo.pipe';

describe('DenominacionActivoPipe', () => {
  it('create an instance', () => {
    const pipe = new DenominacionActivoPipe();
    expect(pipe).toBeTruthy();
  });
});
