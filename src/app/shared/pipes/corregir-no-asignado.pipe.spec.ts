import { CorregirNoAsignadoPipe } from './corregir-no-asignado.pipe';

describe('CorregirNoAsignadoPipe', () => {
  it('create an instance', () => {
    const pipe = new CorregirNoAsignadoPipe();
    expect(pipe).toBeTruthy();
  });
});
