import { EstadoMovimientoCatalogoPipe } from './estado-movimiento-catalogo.pipe';

describe('EstadoMovimientoCatalogoPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoMovimientoCatalogoPipe();
    expect(pipe).toBeTruthy();
  });
});
