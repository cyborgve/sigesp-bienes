import { CorregirReferenciaEstadoPipe } from './corregir-referencia-estado.pipe';

describe('CorregirReferenciaEstadoPipe', () => {
  it('create an instance', () => {
    const pipe = new CorregirReferenciaEstadoPipe();
    expect(pipe).toBeTruthy();
  });
});
