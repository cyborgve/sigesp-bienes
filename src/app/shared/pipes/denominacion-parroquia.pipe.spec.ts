import { DenominacionParroquiaPipe } from './denominacion-parroquia.pipe';

describe('DenominacionParroquiaPipe', () => {
  it('create an instance', () => {
    const pipe = new DenominacionParroquiaPipe();
    expect(pipe).toBeTruthy();
  });
});
