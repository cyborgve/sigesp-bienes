import { CorregirCodigoRetornoPipe } from './corregir-codigo-retorno.pipe';

describe('CorregirCodigoRetornoPipe', () => {
  it('create an instance', () => {
    const pipe = new CorregirCodigoRetornoPipe();
    expect(pipe).toBeTruthy();
  });
});
