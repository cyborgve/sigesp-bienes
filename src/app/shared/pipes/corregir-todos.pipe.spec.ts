import { CorregirTodosPipe } from './corregir-todos.pipe';

describe('CorregirTodosPipe', () => {
  it('create an instance', () => {
    const pipe = new CorregirTodosPipe();
    expect(pipe).toBeTruthy();
  });
});
