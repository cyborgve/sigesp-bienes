import { DenominacionRetornoPipe } from './denominacion-retorno.pipe';

describe('DenominacionRetornoPipe', () => {
  it('create an instance', () => {
    const pipe = new DenominacionRetornoPipe();
    expect(pipe).toBeTruthy();
  });
});
