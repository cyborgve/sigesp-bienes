import { DenominacionColorPipe } from './denominacion-color.pipe';

describe('DenominacionColorPipe', () => {
  it('create an instance', () => {
    const pipe = new DenominacionColorPipe();
    expect(pipe).toBeTruthy();
  });
});
