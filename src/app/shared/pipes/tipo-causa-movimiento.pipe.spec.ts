import { TipoCausaMovimientoPipe } from './tipo-causa-movimiento.pipe';

describe('TipoCausaMovimientoPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoCausaMovimientoPipe();
    expect(pipe).toBeTruthy();
  });
});
