import { NumeroCorrelativoPipe } from './numero-correlativo.pipe';

describe('NumeroCorrelativoPipe', () => {
  it('create an instance', () => {
    const pipe = new NumeroCorrelativoPipe();
    expect(pipe).toBeTruthy();
  });
});
