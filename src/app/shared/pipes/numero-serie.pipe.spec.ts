import { NumeroSeriePipe } from './numero-serie.pipe';

describe('NumeroSeriePipe', () => {
  it('create an instance', () => {
    const pipe = new NumeroSeriePipe();
    expect(pipe).toBeTruthy();
  });
});
