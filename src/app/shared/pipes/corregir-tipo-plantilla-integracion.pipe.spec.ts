import { CorregirTipoPlantillaIntegracionPipe } from './corregir-tipo-plantilla-integracion.pipe';

describe('CorregirTipoPlantillaIntegracionPipe', () => {
  it('create an instance', () => {
    const pipe = new CorregirTipoPlantillaIntegracionPipe();
    expect(pipe).toBeTruthy();
  });
});
