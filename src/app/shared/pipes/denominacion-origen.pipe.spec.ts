import { DenominacionOrigenPipe } from './denominacion-origen.pipe';

describe('DenominacionOrigenPipe', () => {
  it('create an instance', () => {
    const pipe = new DenominacionOrigenPipe();
    expect(pipe).toBeTruthy();
  });
});
