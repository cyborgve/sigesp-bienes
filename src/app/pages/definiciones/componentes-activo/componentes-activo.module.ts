import { NgModule } from '@angular/core';
import { ComponentesActivoRoutingModule } from './componentes-activo-routing.module';

@NgModule({
  imports: [ComponentesActivoRoutingModule],
})
export class ComponentesActivoModule {}
