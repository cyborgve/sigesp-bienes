import { NgModule } from '@angular/core';

import { CorrelativosRoutingModule } from './correlativos-routing.module';

@NgModule({
  imports: [CorrelativosRoutingModule],
})
export class CorrelativosModule {}
